---
spring:
  application:
    name: company
  cloud:
    config:
      uri: ${vcap.services.config-server-uri.credentials.uri}

management:
  security:
    enabled: false

security:
 basic:
   enabled: false  